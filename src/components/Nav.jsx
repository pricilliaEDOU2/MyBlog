const Nav = (props) => {
  return (
    <nav className={props.className}>
      <div>
        <a href="#">Link</a>
        <a href="#">Link</a>
        <a href="#">Link</a>
      </div>
      <div>
        <a href="#">Link</a>
      </div>
    </nav>
  );
};
export default Nav;
